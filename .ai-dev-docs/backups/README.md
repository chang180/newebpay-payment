Backup of removed .ai-dev-docs folders

This folder contains backups of `reports/` and `fixes/` moved during cleanup.

Do not edit these files here; restore them by moving back to the parent folder if needed.
